[package]
name = "iwd-tui"
version = "0.1.0"
edition = "2024"

[dependencies]
crossterm = "0.28"
libc = "0.2"
ratatui = "0.29"
zbus = "5"

[dev-dependencies]
insta = "1"

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
panic = "abort"
strip = true
